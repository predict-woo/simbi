Basically, I want to create an open source desktop notetaking app that live symbiotically with the codex app.

It should be a macos native desktop app. Please do some research on a modern and clean stack first, and set up a skeleton before starting any implementation.

It should work cleanly with codex.

Basically, the app when installed should create a home directory under ~/ .

Then, it should show a file tree on the side that is basically the actual structure of the home directory. 